# FAST-LOCALIZATION 定位工作逻辑与输入输出说明

## 1. 项目定位

FAST-LOCALIZATION 是基于 FAST-LIO2 的**已知三维地图定位**模块：先在预先建立的
三维点云地图中自动找到初始位姿，再持续用 LiDAR、IMU 和固定先验地图估计机器人位姿。

它不是建图节点。全局初始化成功后，先验地图保持固定，不再向其中增量加入当前扫描点。

> RTK 不参与本节点在线定位；RTK 仅用于离线轨迹叠加和误差评估。

## 2. 输入

### 2.1 ROS 在线传感器输入

机器狗 launch 默认订阅：

| 输入 | 默认话题 | 消息/内容 | 作用 |
| --- | --- | --- | --- |
| LiDAR 点云 | `/front_lidar` | `sensor_msgs/PointCloud2`；字段为 `x/y/z/intensity/tag/line/timestamp` | 点云配准、地图约束 |
| IMU | `/front_lidar/imu` | `sensor_msgs/Imu` | 状态传播、点云去畸变和姿态估计 |

LiDAR 到 IMU 的固定外参由 `config/robotdog.yaml` 提供：平移为
`[-0.011, -0.02329, 0.04412] m`，旋转为单位阵；在线运行不估计该外参。

### 2.2 先验地图输入

默认 `map_format=fast_livo2`，`map_dir` 指向一次完整 FAST-LIVO2 三维建图运行目录。
节点会读取：

```text
<map_dir>/keyframes/metadata.yaml
<map_dir>/keyframes/*.pcd
<map_dir>/loop_backend/optimized_keyframe_poses_imu.txt
```

若优化位姿文件缺失，则回退到：

```text
<map_dir>/keyframes/keyframe_poses_imu.txt
```

FAST-LIVO2 的关键帧 PCD 位于各自的 LiDAR 局部坐标系，而位姿是 IMU 位姿。加载时，
模块使用元数据中的 LiDAR–IMU 外参，在内存中计算每帧的地图坐标位姿并合成为全局点云
KD-tree；不需要预先转换地图文件。

### 2.3 关键参数输入

`config/robotdog.yaml` 还控制：点云下采样、IMU 噪声、全局初始化 ICP 阈值、地图滤波
分辨率与轨迹输出目录。`localization_robotdog.launch` 可覆盖：

```text
map_dir          # 三维先验地图目录
map_format       # 默认 fast_livo2
trajectory_dir   # 自动轨迹 CSV/PCD 的输出目录
rviz             # 是否启动 RViz
```

## 3. 定位流程

```text
LiDAR 点云 + IMU
        │
        ├─ 预处理：解析机器狗 PointCloud2、距离筛除、时间处理
        ├─ IMU 传播：去畸变并预测当前 IMU 状态
        │
        ├─【全局初始化尚未成功】临时 LiDAR-IMU 里程计
        │      └─ 当前帧 ScanContext → 地图关键帧候选 → 粗/细 ICP → 一致性确认
        │
        ├─ 注入地图坐标初始位姿
        │
        └─【全局初始化成功】固定先验地图约束的连续定位
               └─ IMU 预测 + 点到平面残差 + 迭代误差状态卡尔曼滤波
                       │
                       ├─ /Odometry、/path、TF、注册点云
                       └─ 自动 CSV 红色轨迹与正常退出时的红色 PCD
```

### 3.1 预处理与 IMU 传播

点云回调解析机器狗 `PointCloud2` 格式；每个点的 `timestamp` 用于扫描内时间处理。IMU
传播预测位置、速度、姿态、陀螺仪偏置和加速度计偏置，并在 LiDAR 更新前完成点云去畸变。

### 3.2 自动全局初始化

启动初期尚不知道机器人位于先验地图的何处，因此系统会暂时运行局部 LiDAR-IMU 里程计，
同时把降采样后的扫描送入全局初始化线程：

1. **ScanContext 检索**：将当前扫描描述子与地图关键帧描述子比较，给出候选关键帧与
   初始偏航估计。
2. **两级 ICP 验证**：先以较大对应距离执行粗 ICP，再以较小对应距离执行细 ICP；细 ICP
   必须收敛且 fitness 不高于阈值。
3. **连续一致性确认**：至少两个候选结果的位置和旋转差必须在配置阈值内，避免单帧误匹配。
4. **位姿注入**：最优候选提供 `T_map_imu`。节点将临时里程计坐标转换到地图坐标，从该时刻
   开始发布和记录地图坐标位姿。

因此，红色轨迹不会覆盖数据播放一开始到“全局初始化确认”之间的那段时间；这表示
初始化覆盖缺失，不等价于后续连续定位误差。

### 3.3 初始化后的连续地图定位

初始化成功后，系统在固定全局点云地图中持续搜索邻域点并构造点到平面残差：

1. IMU 先预测当前状态；
2. LiDAR 点按当前 LiDAR–IMU 外参和状态变换到地图坐标；
3. 在全局 KD-tree 中寻找邻域、拟合局部平面并计算残差；
4. 使用迭代误差状态卡尔曼滤波更新状态；
5. 发布并记录更新后的地图坐标位姿。

这一步是“连续定位”：短时运动由 IMU 保持连续性，LiDAR 与先验地图约束长期位置和姿态。

## 4. 输出

### 4.1 ROS 实时输出

| 输出 | 内容 |
| --- | --- |
| `/Odometry` | 当前地图坐标位姿与协方差；`frame_id` 为 `camera_init`，`child_frame_id` 为 `body` |
| `/path` | 全局初始化成功后的路径消息 |
| `camera_init → body` | 对应的 TF 变换 |
| `/global_map` | 已加载的全局先验点云地图 |
| `/cloud_registered` | 当前扫描在地图坐标系下的注册点云 |
| `/cloud_registered_body` | 当前扫描在机体/IMU坐标下的点云 |
| `/Laser_map` | 局部可视化地图点云 |

当前自动轨迹 CSV 实际写入的是滤波状态 `state_point.pos`，即 IMU 状态位置；它与
RTK 比较时应先把 RTK 天线轨迹补偿到 `imu_front` 参考点。

### 4.2 文件输出

全局初始化确认后，节点自动开始记录；正常以 `Ctrl+C` 退出时写入：

```text
<trajectory_dir>/localization_trajectory_<日期>_<时间>_pid<进程号>.csv
<trajectory_dir>/localization_trajectory_<日期>_<时间>_pid<进程号>.pcd
```

若没有设置 `trajectory_dir`，默认写入 `FAST-LOCALIZATION/Log/`。CSV 字段为：

```text
timestamp_sec,x,y,z,qx,qy,qz,qw
```

PCD 是 RGB 为红色的轨迹点，可与先验三维地图或补偿后的蓝色 RTK 轨迹在 CloudCompare
叠加查看。必须正常关闭节点，强制终止可能导致 PCD 未完成写入。

## 5. 正常运行与常见边界

- 启动定位前，确认 `map_dir` 指向三维先验地图运行目录，而不是二维栅格或场景渲染目录。
- 不要同时启动 FAST-LIVO2 建图节点和 FAST-LOCALIZATION 定位节点来消费同一 LiDAR/IMU
  话题或发布重名位姿。
- 未出现“Global localization confirmed”时，系统仍处于初始化阶段；此时不应将临时局部
  里程计当作最终地图坐标定位结果。
- 全局初始化参数依赖地图结构、点云密度和环境重复性；ICP fitness 阈值并非对所有场景固定有效。
- 当前 RTK 导出、杆臂补偿和 XY 误差统计脚本属于**离线评估流程**，不属于本定位节点的
  在线输入或在线输出。
